<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kacamata.kacamatabacauser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/kacamata"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="false">
        <activity android:name=".ui.testimony.DetailTestActivity"/>
        <activity android:name=".ui.main.DetailNewsActivity" />
        <activity android:name=".ui.main.SplashActivity"
            android:theme="@style/NoAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.profile.ProfUpDelActivity" />
        <activity android:name=".ui.profile.ProfileUpdateActivity" />
        <activity android:name=".ui.home.OrderActivity" />
        <activity android:name=".ui.main.SecondActivity"
            android:theme="@style/NoAct"/>
        <activity android:name=".ui.main.MainActivity"
            android:theme="@style/NoAct"/>
    </application>

</manifest>